import 'package:flutter/material.dart';

class MobileAdminVaultScreen extends StatelessWidget {
  const MobileAdminVaultScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
