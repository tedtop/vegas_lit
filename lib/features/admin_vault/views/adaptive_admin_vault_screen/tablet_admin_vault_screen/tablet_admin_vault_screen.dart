import 'package:flutter/material.dart';

class TabletAdminVaultScreen extends StatelessWidget {
  const TabletAdminVaultScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
