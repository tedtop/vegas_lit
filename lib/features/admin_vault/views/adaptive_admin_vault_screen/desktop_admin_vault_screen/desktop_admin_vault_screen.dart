import 'package:flutter/material.dart';

class DesktopAdminVaultScreen extends StatelessWidget {
  const DesktopAdminVaultScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
