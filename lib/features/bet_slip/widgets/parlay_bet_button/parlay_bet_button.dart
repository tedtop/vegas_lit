import 'package:flutter/material.dart';

class ParlayBetSlipButton extends StatelessWidget {
  const ParlayBetSlipButton({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [],
      ),
    );
  }
}
